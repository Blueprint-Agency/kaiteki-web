// Preview for BranchesExplorer — region-tabbed grid of the 9 branch cards.
// No props; reads the branches dataset and groups by region.
import { BranchesExplorer } from "@/components/BranchesExplorer";

export function Default() {
  return <BranchesExplorer />;
}
