// Preview for DoctorsSlider — horizontally scrollable row of doctor cards.
// No props; reads the doctors dataset.
import { DoctorsSlider } from "@/components/DoctorsSlider";

export function Default() {
  return <DoctorsSlider />;
}
