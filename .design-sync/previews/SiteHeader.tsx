// Preview for SiteHeader — the full-width sticky top navigation. No props; it
// builds the primary nav and mega-menu triggers from site config + content data.
// (Mega-menu panels open on interaction, so the static card shows the bar.)
import { SiteHeader } from "@/components/SiteHeader";

export function Default() {
  return <SiteHeader />;
}
